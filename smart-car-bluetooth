#!/usr/bin/expect -f

set timeout 50

spawn "bluetoothctl"
expect "#"
send "discoverable on\r"
expect "Changing discoverable on succeeded"

expect {
	timeout {puts "timed out"; exit}
	"Confirm passkey *"
	"Authorize service *"	
}
send "yes\r"
     
expect "#"

